# CLAUDE.md

## Code style

- No comments in code. Only add a comment when it explains something that
  cannot be understood by reading the code itself (a non-obvious constraint,
  a workaround, a business rule that isn't visible in the logic). Never add
  comments that restate what the code does.
